# 更新记录

## v12.9.2 — 第十关追踪装置收尾

- 第十关（掌声向上）的自动追踪装置在玩家安全越过设备后停止发射。
- 已清除该装置已经生成的飞行物，并在画面上显示“已失效”，避免过关后仍被背后追击。
- 通过配置字段控制，不影响其他关卡的追踪设备。


## v12.9.1 — 第 13 关聚光灯调优与入口稳定化

- 第 13 关（掌声计量器）移除一束额外聚光灯，保留主机制、警告节奏与检查点，减少光束重叠造成的误判。
- 聚光灯数量调整改为关卡配方数据字段，后续 AI 或关卡设计可继续精确调参，不在主循环里写房间特判。
- 同步生成 GitHub Pages 的 `index.html` 与 `play.html`，`play.html` 作为不变的备用入口。


## v12.9.0 — 剧情清晰化

- 新增统一叙事内容源 `source/src/story/narrative.ts`，将序章、24 个普通房间、4 个 Boss 和终章串成一条因果线。
- 每个房间卡显示说话者与关键台词，暂停页显示这一段的剧情目标；玩家不需要先读档案才能理解“为什么要继续向右”。
- 明确揭示四个核心答案：主角是设计师、乐园是服从实验、笑脸园长是执行人格、收集记忆决定如何醒来。
- 重写四种结局文案，说明收集行为与最终选择的关系。

## v12.8.0 — 流畅碰撞与障碍分离

- 固定步碰撞扫描改为无临时数组热路径，减少频繁垃圾回收引发的卡顿。
- 粒子生成按剩余容量裁剪，数量硬上限保持 70，连锁机关不会造成瞬时堆积。
- 手机紧凑视口与减少动态效果模式自动启用轻量绘制；移除平台、尖刺逐个模糊阴影，保留纸雕层次和清晰轮廓。
- 新增尖刺碰撞体、动态扫掠范围与平台/锁墙/支路冲突校验；不自然穿模的生成危险会被清理。
- 性能验收增加 Canvas 绘制、固定步更新和粒子上限门槛；现为 28 个测试文件、104 项测试。

## v12.7.0 — 按钮安全与逻辑陷阱更新

- 修复“最后的笑脸”中右侧黄色机关被隐藏尖刺覆盖、无法安全触碰的问题。
- 新增按钮安全区：静止尖刺、移动尖刺、轨道尖刺、激光和压台机的完整扫掠范围均不得侵入按钮。
- 关卡生成会自动移除冲突危险并同步清理失效陷阱目标，防止再次产生软锁。
- 平台装饰尖刺改为落点边缘或跳跃间隙布置，不再任意压在按钮和落脚中心。
- 第三关起加入章节化小坑：糖牙伏击、飞刀换位、镜像轨道和钟表红线，前两关继续保留教学空间。
- 最终 Boss 压台机移出阶段按钮路径；未按要求躲完攻势仍不能推进。
- 新增按钮动态扫掠、全房间安全和章节陷阱测试；现为 28 个测试文件、103 项测试。

## 仓库交接与自动部署增强 — 2026-08-11

- 确认 `source/` 已包含当前游戏全部 TypeScript、HTML、CSS、测试和构建脚本，且与正式单文件一致。
- 新增 `AGENTS.md`、`AI_HANDOFF.md` 和机器可读 `docs/project-state.json`，让其他 AI 无需历史对话即可接手。
- 新增可移植 Chrome 浏览器验收：递进结构、手机触控和性能采样。
- 新增发布 SHA-256 一致性校验，避免源码、构建和根页面版本漂移。
- GitHub Pages 改为 Actions 自动测试、构建和部署；推送个人仓库 `main` 后自动更新公开链接。

## v12.6.0 — 递进闯关版

- 完整移除“险过连拍”和“舞台热度”的 HUD、设置、合同、评级、存档写入与音效反馈。
- 每章六个普通房间明确标记为入门、练习、组合、变化、考核、终局。
- 每个普通房间内部固定显示学习、练习、考验、终点四段，HUD 直接给出当前目标与阶段编号。
- 障碍压力改为逐级增加：第一关不放远程炮台，第二至四关使用一座，第五、六关使用两座；执行机关和终段危险同步逐级加入。
- 24 个额外挑战只保留无伤、限时和不要久停三种可直接理解的规则。
- 检查点新增起点旗、练习票、考验信标和终段皇冠四种轮廓，并显示当前编号。
- 新增递进压力、关卡阶段和检查点视觉层级测试；现为 27 个测试文件、98 项测试。
- 高压房间自动化性能实测约 60 FPS，P99 帧间隔 17.5ms。

## v12.5.0 — 友好陷阱版

- 首页主入口精简为四个等宽按钮；章节、存档、模式、档案与制作信息统一收进“更多内容”。
- 序章加入场景内新手指示牌，说明 WASD、二段跳、下穿薄板、重开与残影管理。
- 键盘新增 `S/↓` 下穿单向薄板，手柄与手机触控同步加入下穿操作。
- 鼠标右键可只删除点中的一具死亡残影；退格仍保留全清功能。
- 复活前只清除会占据复活安全区的残影，避免实体碰撞把玩家挤进墙体，同时保留路线上的其他残影。
- 普通关卡动作锁后的检查点移动到恢复平台顶部；必经平台适度加宽，序章尖刺与追逐压力轻微下调。
- 新增残影管理、存档落点、序章引导与友好落点测试；现为 26 个测试文件、96 项测试。

## v12.4.0 — 桌面手机双端版

- 手机和平板横屏自动显示双拇指触控，桌面继续使用键盘或手柄。
- 触控支持多点同时移动/跳跃、方向区滑动换向、取消/失焦自动松键与可选震动。
- 设置新增触控自动/开启/关闭、方向键大小、跳跃键大小、透明度、底部距离和左右手布局。
- 加入刘海/圆角安全区、竖屏旋转提示和手机横屏紧凑菜单/HUD。
- 手机使用 1280×720 内部画布稳定帧率，桌面继续保持 1600×900 至 1920×1080。
- 新增触控策略与移动画布单元测试；现为 24 个测试文件、90 项测试。

## v12.3.0 — 高清流畅版

- 删除 0.85 倍低分辨率渲染。
- 内部画布改为 1.25–1.5 倍：1600×900 至 1920×1080。
- 背景、世界布景与前景缓存同步使用高清分辨率。
- 恢复完整平台渐变、尖刺折面和角色光影。
- 新增高清渲染比例单元测试。
- 保留固定 60Hz、视野裁剪、粒子上限和后台降频。

## v12.2.0 — 流畅稳定版

- 固定 60Hz 物理。
- 缓存舞台背景、世界布景和前景幕布。
- 降低暂停页与标题页刷新频率。
- 优化尖刺、平台、激光和粒子绘制路径。

## v12.1.0 — 稳定镜头版

- 远景固定，不再随玩家产生视差漂移。
- 摄影机加入 40%–56% 横向死区。
- 移除方向切换造成的镜头甩动。
- 降低受击和死亡震动。

## v12.0.0 — 纯跑跳剧场版

- 移除冲刺、记忆锚点和记忆回溯。
- 24 个普通房间扩展为 3440 像素宽四幕结构。
- 全部随机传送门关闭。
- Boss 阶段不能跳过。
