{
  "name": "lost-memory-park",
  "version": "12.8.0",
  "description": "《失忆乐园》原创非像素递进式纯跑跳、流畅碰撞热路径与障碍安全校验双端网页游戏",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/make-standalone.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "check:release": "node scripts/check-release-sync.mjs",
    "verify": "npm test && npm run build && npm run check:release",
    "qa:browser": "npm run build && node scripts/run-browser-qa.mjs",
    "release:pages": "npm test && npm run build && node scripts/sync-pages.mjs && npm run check:release"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.7"
  }
}
